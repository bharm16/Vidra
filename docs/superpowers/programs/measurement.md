# Measurement Program

Vidra's program to measure and evaluate every surface of the app — operational telemetry (latency, cost, errors) plus quality telemetry (eval scores, judge ratings) — into PostHog, with traffic-source discrimination so the data stays valid pre-launch, during early-user phases, and at scale.

**Status:** active. Sub-projects #0 (Eval Visibility), #1 (Source Discriminator + Synthetic Harness), and #3 (LLM Judge Framework) shipped. #2/#4/#6/#7 not started; #5 dormant under the ADR-0002 freeze (see 2026-07-01 Reordering entry). #3 follow-up: set GH Actions secrets so the nightly crons run — calibration seeding already shipped via Sub-projects A/D/D2; secrets re-verified absent (`total_count: 0`) on 2026-07-01.

---

## Operating context (READ THIS FIRST)

**Vidra has zero real users today.** This entire program exists to validate quality _before_ launch — not to monitor real users (there are none). Every sub-project is grounded in this constraint:

- **There is no organic traffic to learn from.** Pre-launch, the only available signals are **eval data** (deterministic, repeatable, generated by `scripts/evaluation/`) and **dogfood data** (the team using the app on purpose to test).
- **The synthetic-traffic harness (#1) is not optional decoration** — it exists because there is no other way to produce operational telemetry before launch. Without it, dashboards for live surfaces stay empty.
- **Source discrimination (#1)** is preparatory: when real users eventually arrive, their traffic must not be already polluted by dev/CI/dogfood events on the same tiles.
- **Quality eval frameworks (#0, #3)** answer "is the app producing good output?" without needing users to tell us.

If a sub-project discussion ever drifts toward "what are real users doing?" — stop. There are no real users. The question is always **"what signal can we generate ourselves to know we're ready _for_ users?"**

---

## Defer nothing (program-wide scope)

Every endpoint, every background worker, every client-side action, every dollar of LLM/render spend, and every state change with money implications must be observable in PostHog. Not "the high-leverage subset." Not "the surfaces with users." Pre-launch full coverage is achievable because there's no traffic constraint and no migration cost — instrumenting now is cheaper than retrofitting after launch when surfaces have shipped without telemetry.

This expands the original 5-sub-project decomposition (§ Decomposition). New sub-projects #5–#7 cover background workers, client-side analytics, and cost/revenue tracking — surfaces the original framing treated as out-of-scope or implicitly deferred. They are not.

The "defer nothing" rule applies to scope (everything gets instrumented) AND to dimensions (every event has both **health** fields — latency, error rate, $ cost — and **quality** content fields — the inputs/outputs of the operation, scorable by human scroll-through today and LLM judge tomorrow).

**Amended 2026-07-01:** [ADR-0002](../../adr/0002-vidra-is-an-authoring-tool-for-non-experts.md) froze the generation-economics and multi-shot stacks after this directive was written, and the two were never reconciled. "Defer nothing" now reads **"defer nothing within the active authoring loop"** — frozen surfaces get instrumented at unfreeze, not before. Concrete impact on #2/#5/#7 is recorded in the 2026-07-01 Reordering entry.

---

## Quality requires content, not counts (a hard-won principle)

Operational telemetry that only counts things ("how many spans came back", "how long did it take", "how often did it error") cannot answer the question pre-launch quality validation actually needs to answer: **"is the output any good?"** A dashboard tile that shows "3 spans=5 calls, 4 spans=5 calls, 5 spans=4 calls" tells you nothing about whether the spans are correct.

Every operational and eval event in Vidra carries **content fields** alongside the metadata:

- `optimize.completed` → `inputPrompt`, `outputPrompt` (the user's prompt and the optimized result, side-by-side)
- `suggestions.completed` → `highlightedText`, `fullPrompt`, `suggestions` (what the user selected and the alternatives returned)
- `label-spans.completed` → `inputText`, `spans` (text + category per labeled span)
- `eval.completed` → `examples[]` (per-prompt arrays — predicted vs ground-truth for F1, spans + score + notes for judge, drifted flag for recommendation)

Dashboards expose this via "exploded" HogQL tables (`ARRAY JOIN JSONExtractArrayRaw(assumeNotNull(...))`). The user can scroll through 100 examples and form a judgement — that's the only quality signal that matters before LLM judges (sub-project #3) can run live.

**Rule:** any new telemetry event in this program MUST carry enough content for a human to evaluate quality without leaving PostHog. Count-only dashboards are by definition incomplete.

---

## How to use this doc

This is the connective tissue across multiple specs. The work decomposes into 5 sub-projects (§ Decomposition); each gets its own spec and plan in `docs/superpowers/specs/` and `docs/superpowers/plans/`. **Read this doc first** at the start of any session touching measurement work — it's the only place the end state lives in one piece. Every sub-project spec opens with a pointer back here.

When the program's ambition shifts (priorities reorder, surfaces added/removed, axes added), the change lands **here first**, then propagates outward to in-flight sub-project specs — not the reverse. The Reordering log below is the audit trail.

---

## End State

Every user-facing endpoint, every background worker, every client-side interaction, and every async pipeline in Vidra emits **health telemetry** (per-stage latency, $ cost, errors, state changes) AND **quality telemetry** (the input/output content of each operation, plus LLM-judge scores once #3 ships) to PostHog. Operational events carry a `source` discriminator distinguishing real users from `synthetic` / `ci` / `dev` / `unknown` traffic (and `dogfood` once real users exist and a team-member allowlist makes sense to add), so the dashboard stays valid pre-launch, during early-user phases, and at scale. Eval events use a distinct event name (`eval.completed`) and don't need the discriminator. One dashboard per surface (T2V, I2V, Preview, Generation, Eval, Background Workers, Cost/Revenue, Client) answers BOTH _"is this surface healthy?"_ AND _"is the output any good?"_ — every dashboard has both health tiles AND content-review tiles, with LLM-judge score trends layered on top once #3 ships. PostHog alerts catch latency, cost, error-rate, eval-score regressions, AND quality-score regressions before users notice. The question _"is the 4+ LLM-call cost per Optimize click justified?"_ — and its equivalents for every other surface — is answerable on demand, as is _"is the output worth the cost?"_

### "Tell when you're done" checklist

- [x] Three eval scripts emit `eval.completed` events with per-category metrics; "Eval Health" dashboard exists with regression alerts (#0 ✓)
- [x] Every operational event has a `source` property — auto-stamped by the PostHogClient wrapper from request-context ALS (#1 ✓)
- [x] Every operational + eval event carries **content fields** for quality review, not just counts (added 2026-05-12; see "Quality requires content, not counts")
- [x] **LLM judge scores live `<surface>.completed` events and emits `quality.scored`** with per-dimension breakdowns; nightly cron + PR calibration gate workflows exist (#3 ✓ — see 2026-05-13/14 entries in Reordering log for the multi-layer fixes the rollout surfaced)
- [x] **Synthetic harness invokes real services** (not canned outputs) so judge scores reflect production LLM quality (added 2026-05-14; pattern in `scripts/synthetic/utils/aiService.ts`)
- [ ] Every endpoint listed in the `CLAUDE.md` Route → Service → Client API Map emits at least one `<surface>.completed` event _(3/13 done — Optimize, Suggestions, Span Labeling; #2 covers the rest of the active loop; frozen-stack routes — continuity, payment — deferred per ADR-0002, 2026-07-01)_
- [ ] Every endpoint emits per-stage `llm.call.completed` events tagged with `executionType` _(falls out of #2 — telemetry services wrap `aiService.execute` automatically)_
- [ ] Each of {T2V, I2V, Preview, Generation} has a PostHog dashboard with content-review tiles _(T2V ✓; rest pending #2 + #4)_
- [ ] At least one PostHog alert per dashboard, exercised in test _(Eval Health ✓; rest pending)_
- [ ] **Judge calibration: ρ ≥ 0.7 against hand-labeled examples per surface** _(seeded via Sub-projects A/D/D2; clean-data ρ = 0.433/0.507/0.525 with MAE/ceilingPct diagnostics per D2 — ρ-only gate unreliable on ceiling-clustered surfaces; see Reordering log)_
- [ ] **GitHub Actions secrets** set so the nightly `Quality Judge` AND `span-labeling-eval` crons actually run _(re-verified `total_count: 0` on 2026-07-01 — both nightlies silently dead; needed: `OPENAI_API_KEY`, `POSTHOG_API_KEY`, `POSTHOG_PERSONAL_API_KEY`, `POSTHOG_PROJECT_ID`, `POSTHOG_HOST`, `GROQ_API_KEY`)_

---

## Decomposition

The work breaks into 5 sub-projects. Each is independently designed (own spec + plan + ship cycle); the dependency arrows in § Sequencing govern ordering, not bundling.

| #     | Sub-project                                          | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Status      | Spec                                                                                                                      | Plan                                                                                                        |
| ----- | ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| **0** | **Eval Visibility**                                  | Pipe the three existing eval scripts (`span-labeling-evaluation.ts`, `golden-set-relaxed-f1.ts`, `recommendation-eval.ts`) into PostHog as `eval.completed` events with `evalType` discriminator. Eval Health dashboard, regression alerts. Plus (added 2026-05-12) per-prompt `examples[]` so dashboards can drill from "F1 dropped" to "these specific prompts caused it."                                                                                                              | **shipped** | [`2026-05-10-eval-visibility-design.md`](../specs/2026-05-10-eval-visibility-design.md)                                   | TBD                                                                                                         |
| **1** | **Source discriminator + synthetic-traffic harness** | Adds `source: "user" \| "synthetic" \| "ci" \| "dev" \| "unknown"` (5 values; `dogfood` deferred until real users exist) to every operational event payload, auto-stamped by the PostHogClient wrapper from the request-context ALS. New `label-spans.completed` surface event. New `scripts/synthetic/` harness driving Optimize / Suggestions / Span Labeling. Three new dashboards (Suggestions Health, Span Labeling Health, LLM Calls Health).                                       | **shipped** | [`2026-05-10-source-discriminator-and-harness-design.md`](../specs/2026-05-10-source-discriminator-and-harness-design.md) | [`2026-05-10-source-discriminator-and-harness.md`](../plans/2026-05-10-source-discriminator-and-harness.md) |
| **2** | **Operational telemetry coverage**                   | Extend the existing `*Trace` + `<surface>.completed` pattern to: I2V motion (`/api/motion`), preview (`/api/preview/*`), final generation, span labeling (`/llm/label-spans`), model intelligence (`/api/model-intelligence`). Continuity dropped 2026-07-01 per ADR-0002.                                                                                                                                                                                                                | not started | TBD                                                                                                                       | TBD                                                                                                         |
| **3** | **LLM-effectiveness eval framework**                 | GPT-4o-as-judge with per-surface rubrics for Optimize (5 dims), Suggestions (5 dims), Span Labeling (5 dims). Emits `quality.scored` events with per-dimension breakdown + reasoning. Per-source sampling (synth/dogfood 100%, user 10% pre-launch). PostHog HogQL read client + nightly cron + PR calibration gate workflow. Rubrics pinned to `shared/taxonomy.ts` v3.0.0. **Code shipped 2026-05-12 → 2026-05-14**; calibration data seeding + GH secrets remain (see Reordering log). | **shipped** | [`2026-05-12-llm-judge-framework-design.md`](../specs/2026-05-12-llm-judge-framework-design.md)                           | [`2026-05-12-llm-judge-framework.md`](../plans/2026-05-12-llm-judge-framework.md)                           |
| **4** | **Cross-surface dashboards + alerts**                | Build T2V Health, I2V Health, Preview Health, Generation Health dashboards (Eval Health is part of #0); wire PostHog alerts on threshold violations across surfaces.                                                                                                                                                                                                                                                                                                                      | not started | TBD                                                                                                                       | TBD                                                                                                         |

### Already shipped (foundation)

The T2V Optimize telemetry stack already exists and is the pattern subsequent sub-projects replicate:

- `optimize.completed` — per `/api/optimize` call ([spec](../specs/2026-05-09-t2v-optimize-telemetry-design.md))
- `llm.call.completed` — per `aiService.execute` invocation
- `suggestions.completed` — per `/api/enhancement/suggestions` call
- "T2V Optimize Health" dashboard (PostHog project `417445`, dashboard id `1565688`)
- Documented in [`docs/architecture/observability.md`](../../architecture/observability.md)

#0 (Eval Visibility) adds a new event family (`eval.completed`) flowing from `scripts/evaluation/` into the same PostHog project. #1 retrofits the source discriminator onto the existing operational events. #2 replicates the operational pattern to ALL remaining server route surfaces (defer nothing). #3 introduces LLM-judge scoring for live (non-eval) traffic — runs against any event that carries content fields, so it can start on Optimize/Suggestions/Span Labeling before #2 finishes. #4 builds cross-surface dashboards. #5 instruments the 7 background workers — health cycles + state-change content arrays for verification. #6 wires `posthog-js` client-side analytics for dogfooding (now) + real users (post-launch). #7 adds $ cost tracking to every LLM/preview/render event and a Cost/Revenue dashboard.

---

## New sub-projects #5-#7 (added 2026-05-12 under "defer nothing")

| #     | Sub-project                     | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Status                                                                                                                                     | Spec | Plan |
| ----- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ---- | ---- |
| **5** | **Background worker telemetry** | One `worker.cycle.completed` event per cycle for all 7 workers (`VideoJobWorker`, `VideoJobSweeper`, `VideoJobReconciler`, `DlqReprocessorWorker`, `WebhookReconciliationWorker`, `BillingProfileRepairWorker`, `CreditReconciliationWorker`, `CreditRefundSweeper`). Carries health (cycleDurationMs, itemsProcessed, outcome) AND state-change content arrays — e.g., `reconciledUsers[]: {userId, beforeBalance, afterBalance}` so money correctness is reviewable. Shared `WorkerTelemetryService`. "Background Workers Health" dashboard. Alert on "worker silent > 2× expected interval" — the highest unaddressed silent-failure risk in the codebase. | **dormant** (ADR-0002 — all 8 workers are frozen-stack; 2026-07-01 entry)                                                                  | TBD  | TBD  |
| **6** | **Client-side analytics**       | Initialize `posthog-js` in the React client. Captures page views, button clicks via `posthog.capture`, unhandled errors, time-on-page, feature usage. Pre-launch this captures own-dogfooding sessions (today, only Bryce — useful for personal QA). Post-launch this surfaces real user behavior. Pairs with #1's `source` discriminator: client events default to `user` source; dogfood UIDs override to `dogfood` once allowlist activates.                                                                                                                                                                                                               | not started                                                                                                                                | TBD  | TBD  |
| **7** | **Cost / revenue telemetry**    | Adds `costUsd` field (computed from a per-provider/per-model pricing table) to every `llm.call.completed`, every preview-generation event, every final-render event. Worker events from #5 surface Stripe revenue movement (refunds, charges, credit grants). New "Cost / Revenue Health" dashboard answers: monthly burn rate, cost-per-click by surface, $-per-quality-point once #3 ships. Critical pre-launch to know "are we shipping a profitable product?"                                                                                                                                                                                             | not started — active-loop slice only (`costUsd` on LLM/preview/render events); Stripe-revenue slice dormant with #5 (ADR-0002; 2026-07-01) | TBD  | TBD  |

---

## Sequencing

**Updated 2026-05-14:** #0, #1, #3 are shipped (code). Active sequencing:

- **#2 (server route telemetry coverage) next.** Adds `<surface>.completed` to the active-loop routes still untracked (I2V motion, preview, generation, model intelligence, sessions, assets, reference images, capabilities, storage). Continuity dropped per ADR-0002 (2026-07-01 entry). The judge layer from #3 attaches to each as it lands — every new content-carrying event becomes judgeable for free. The structural constraint set by #3 (`candidate.category` field, structural taxonomy lookups) means #2 surfaces must emit category-typed content; #3's rubrics for new surfaces follow the same 5-dimension pattern.
- **#6 (client) and #7's active-loop cost slice in parallel with #2.** They touch different code paths and can ship in independent worktrees. #5 is dormant and #7's Stripe-revenue slice frozen with it — all 8 named workers belong to the ADR-0002 frozen stacks (2026-07-01 entry).
- **#3 follow-up in parallel:** set GH Actions secrets so the nightly crons actually run (re-verified `total_count: 0` on 2026-07-01). Calibration seeding shipped via Sub-projects A/D/D2 — the earlier "seed calibration JSON" item here was stale.
- **#4 (cross-surface dashboards + alerts) last.** This is the consolidating pass — by the time it starts, every individual surface dashboard already has health + content + quality tiles. #4's value is roll-ups + alert calibration (latency, cost, error-rate, AND quality-score regressions), not first-time tile-building.

**Historical note:** original sequencing had #3 after #2 (a "you can only judge after surfaces are instrumented" assumption). The 2026-05-12 content-fields work moved this — three surfaces became judgeable immediately, so the cost of waiting outweighed the cost of starting. That bet paid off: #3 shipped within two days of resequence and surfaced four false-signal layers (rubric phantom taxonomy, canned harness output, regex classifiers in scorer, mechanical bugs in the structural rewrite) that would have been compounded with #2's new surfaces if discovered later.

### Reordering log

- **2026-05-10:** Original ordering had source discriminator at #0 because I treated user-traffic telemetry as the throughline. User reframed: the goal is _eval visibility_, not source tagging. Reordered so Eval Visibility is #0, source discriminator is #1 (deferred). The dependency `#0 → #1` is gone because eval events have a distinct event name (`eval.completed`) and don't share the source-discrimination concern with operational events.
- **2026-05-12:** Two corrections after #1 shipped. (a) The synthetic harness fired HTTP requests against authenticated endpoints, 401'd, produced zero events. Rewrote to direct in-process emission through the same PostHogClient + telemetry services the server uses, wrapped in an ALS frame with `source: "synthetic"`. Verified end-to-end with 174 real events. (b) Dashboards were count-only ("3 spans=5 calls") — useless for quality review. Added **content fields** (`inputPrompt`/`outputPrompt`, `spans[]`, `suggestions[]`, `examples[]` for evals) to every event so dashboards can show what the model actually produced. New principle encoded in "Quality requires content, not counts" above. Affects all subsequent sub-projects: any new event in this program MUST carry content, not just metadata.
- **2026-05-12 (later):** "Defer nothing" scope directive. Added sub-projects #5 (background worker telemetry), #6 (client-side analytics via `posthog-js`), #7 (cost/revenue tracking) — surfaces the original 5-sub-project decomposition treated as out-of-scope. Also resequenced: #3 (LLM judge framework) now ships BEFORE #2, because Optimize/Suggestions/Span Labeling are already judgeable from the content fields shipped on 2026-05-12, and the judge schema becomes the design constraint for every subsequent #2 surface.
- **2026-05-13:** #3 judge code shipped (rubric loader, OpenAI judge client, PostHog HogQL read client, run-judge orchestrator, per-surface isolation, rubrics v1 with prompt-injection defense, calibration runner + placeholder JSON, nightly + PR-gate CI workflows; observability doc updated). First end-to-end run surfaced two false-signal layers above the judge: (a) **rubric phantom-taxonomy drift** — span-labeling rubric used `mood`/`setting` example categories that don't exist in `shared/taxonomy.ts` v3.0.0, so categoryAccuracy floored at 1.55/5 against invented categories; fixed by enumerating the canonical 9 parent categories + attribute IDs directly in the rubric prose, plus adding a `targetModel=null` policy to the Optimize rubric so synthetic events with no target model stop bottoming-out constraintCompliance. (b) **Calibration JSON is placeholder** — all three `*.calibration.json` files are empty arrays, so the PR calibration gate workflow passes vacuously; surfaces a structural-trust gap that doesn't change scores but makes them un-anchored. Encoded principle: _measurement-system layers (rubric → harness → scorer → judge) each carry their own classification risk and need explicit verification._
- **2026-05-14:** Two more false-signal layers fixed under #3. (a) **Synthetic harness emitted canned content via `synthesize*()` helpers** — drivers had string pools that produced fixed outputs regardless of `fullPrompt`, so the judge was scoring canned synthesis, not real service quality. Refactored all three drivers (`scripts/synthetic/drivers/*.driver.ts`) to invoke live services: `labelSpans()` via Gemini, `EnhancementV2Engine.execute()`, `PromptOptimizationService.optimize()`. New `scripts/synthetic/utils/aiService.ts` mirrors the prod DI registration in `server/src/config/services/llm.services.ts` (groq+qwen share a single `ConcurrencyLimiter`; timeouts match prod `env.ts` defaults: `GROQ_TIMEOUT_MS=5000`, `GEMINI_TIMEOUT_MS=30000`). Result: span-labeling score 12.73 → 19.46 (categoryAccuracy 1.30 → 3.36) on real Gemini output. (b) **V2CandidateScorer carried regex-based classification gates** — `SEMANTIC_FAMILY_PATTERNS` enumerated trigger words per family, so valid LLM output ("saxophonist", "barista", "pouring milk") got rejected as `family_miss` (subject_identity regex lacked those terms) or `grammar` (verb_phrase regex lacked those stems). Empty rejection rate measured at 60% on the suggestions surface before fixes. Rewrote `isCategoryRequiredMatch` / `isCategoryForbidden` / `_matchesGrammar` to use structural taxonomy lookups via the LLM's self-declared `candidate.category` field + `getParentCategory` from `shared/taxonomy.ts`; deleted `_hasBodyPartDrift` and `_hasObjectOverlap` entirely (their work is now covered by the EnhancementV2PromptBuilder scene-coherence + drop-in-substitution rules). Deleted the `policies/semanticFamilies.ts` file (0 consumers after the rewrite). Empty rate dropped to 0%; suggestions avg 12.91 → 16.59 (+3.68). New rule encoded in memory: **wordlists / Set+phrase lookups are regex in another form** — when refactoring a text classifier, use the LLM's self-classification + structural taxonomy lookup, not a more-local data structure.
- **2026-05-14 (Layer 5):** Fifth measurement-system false-signal layer: synthetic suggestions fixture self-consistency. The 2026-05-14 baseline scored suggestions at 16.02/25, dragged by relevance (2.53) and categoryFidelity (2.49) — both due to the suggestions driver deriving `highlightedText` from `prompt.text`'s first two words and `highlightedCategory` from `prompt.tags[0]`, producing contradictory pairs like (`"Time-lapse of"`, `camera.focus`). The engine honestly produced `camera.focus` suggestions; the judge correctly penalized them as off-context. Fixed by extending fixtures with hand-authored `highlights: [{ text, category }][]` (20 prompts, 58 highlights), validated at driver startup against `shared/taxonomy.ts` v3.0.0 via a new `scripts/synthetic/utils/fixture-validation.ts` module with 6 TDD tests. Post-fix (n=53 scored): total 20.06 (+4.04), categoryFidelity 4.19 (+1.70, prediction confirmed: artifact was the dominant driver), relevance 3.57 (+1.04, partial lift — residual gap is real engine behavior, not artifact), plausibility 4.70 (+0.33), diversity 3.83 (+0.41), qualityRange 3.77 (+0.56), minTotal 5 (up from 2). Split outcome: categoryFidelity's gap was almost entirely artifact, but relevance's residual gap is real — the engine sometimes generates category-correct suggestions that don't fit the surrounding prompt narrative. That's the Phase 2 target. New rule encoded: _fixture pairings that derive multiple consumer inputs from independent fixture fields are themselves classifiers — same risk as rubric / harness output / scorer layers, and need the same explicit verification._
- **2026-05-21 (Sub-project B, partial pass):** Suggestions scene-summary-first shipped. EnhancementV2 prompt now instructs the LLM to emit a one-sentence `scene_summary` before the suggestions array; schema declares the field (required for OpenAI strict, properties-only for Groq simplified). Mechanism: autoregressive conditioning — the LLM's own summary tokens are in context while it generates each suggestion, so scene-specific constraints are enforced by the model's recent generation history rather than by re-reading the rules section. Implementation expanded `StructuredOutputEnforcer.enforceJSON` with an opt-in `captureSiblings` option to surface scene*summary into V2 engine telemetry without breaking the existing unwrap-to-array contract. Post-change baseline (n=47, calibrated judge ρ=0.755): relevance 3.87 (+0.30 from 3.57), categoryFidelity 4.51 (+0.32), plausibility 4.70 (flat), diversity 4.06 (+0.23), qualityRange 3.83 (+0.06), total 20.98 (+0.92), **minTotal 11 (up from 5)**. Success criterion not fully met (target was relevance ≥ 4.3, total ≥ 22) but four of five dimensions lifted with no regression below floors, and the worst-case score nearly doubled. Root cause of capped lift: Qwen via Groq emits `scene_summary` only ~36% of the time despite the prompt instruction (Groq json_object mode doesn't honor required-arrays the way OpenAI strict mode does); the mechanism fires only for that ~36%, averaging the lift down. **Two implementation-time bugs surfaced and fixed:** (a) Initial Groq schema marked scene_summary as required → Qwen's drop rate caused 14 of 43 events to fail validation as "Missing required field"; fixed by moving the field to `properties` only in the Groq variant while keeping it required on OpenAI strict (decoding-level enforcement). (b) `StructuredOutputEnforcer.enforceJSON` had no way to surface sibling fields from the parent object before unwrapping; added opt-in `captureSiblings` option with 2 TDD tests. New principle encoded: \_LLM emission rate of a schema-documented field varies dramatically by provider — Groq json_object mode treats `required` as advisory, OpenAI strict mode treats it as grammar-constrained. Schemas that work cross-provider must split on this; the prompt instruction is the only universal driver of emission, and the engine must be tolerant of partial compliance from weaker JSON-mode implementations.* The minTotal jump (5→11) is the strongest signal that the mechanism does what the spec hypothesized when it fires: when scene_summary is present, the worst-case off-context failures disappear. Next step: Sub-project B2 to push Qwen emission rate from ~36% toward 80%+ (stronger prompt language, one-shot example, or model swap for the primary suggestions path).
- **2026-05-21 (Sub-project E, shipped):** Synthetic model matrix infrastructure shipped — the harness can now run any surface across N (provider, model) variants in one invocation and produce a comparison report tagged at the event level. Four new entrypoints: `scripts/synthetic/variants.ts` (typed preset registry, 8 seed presets across the 3 surfaces — span-labeling/suggestions/optimize × qwen/gemini/openai pairings), `scripts/synthetic/run-matrix.ts` (subprocess orchestrator, sequential per variant so one variant's failure can't poison another's run), `scripts/synthetic/report-matrix.ts` (HogQL comparison query, CTE-based aggregation with winner annotation), plus a `--variant-tag` flag on `run-harness.ts` that threads through ALS into a new `modelVariant?: string \| null` field on all three surface telemetry events. The span-labeling driver's previously hardcoded `PROVIDER`/`MODEL` constants now read from `SPAN_PROVIDER`/`SPAN_MODEL` env vars matching the names in `server/src/config/modelConfig.ts:391-392` — same dispatch keys, single source of truth. First real cross-model data (suggestions, n=60 qwen vs n=41 gemini): qwen total 21.18 vs gemini 20.32, **winner qwen by +0.87**. Caveat: gemini's lower n is not a Sub-project E bug — Gemini's JSON-mode produced ~17 unparseable structured-output responses in `EnhancementV2Engine._generateGuidedCandidates`, pre-existing provider flakiness flagged for separate triage. **Task 8's HogQL discovery:** the report's initial query (unbounded JOIN + JSONExtract + post-join `IS NOT NULL` filter) timed out on real PostHog data despite passing against the synthetic dev fixture; rewrote as a CTE that filters before the join and the query returns sub-second. **Two non-Sub-project-E concerns surfaced during e2e verification, deferred for separate triage:** (i) Gemini structured-output JSON parse flakiness in `EnhancementV2Engine._generateGuidedCandidates` (the n=41/58 cause above); (ii) judge-dedup race when `npm run judge:run` is invoked rapidly twice — silent stdout makes re-invocation tempting, fix is a "scored N events" log line so the operator knows the first call already drained the queue. This unblocks **Sub-project B2** (Qwen vs Gemini emission-rate comparison): the question "should B2 raise Qwen's ~36% scene\*summary emission rate or swap to Gemini?" is now empirically answerable, and the first matrix data already provides preliminary signal — Qwen with partial scene-summary emission still beats Gemini head-to-head, so B2 should target raising Qwen's emission rate rather than swap providers. Two new principles encoded: _measurement tooling that exists in two places (the routing layer in `modelConfig.ts` and the telemetry annotation in driver files) must read from the same env vars; drift between them produces silently-wrong telemetry that misattributes results to the wrong model, and e2e verification surfaces bug classes unit tests can't catch — query plans against synthetic data differ from query plans against production data, and any measurement query that touches real event volume must be smoke-tested against production cardinality before it's trusted as a comparison report._
- **2026-05-15 (Sub-project A, partial pass):** Quality-judge calibration seeded. 60 stratified events (20 per surface × 3 surfaces) labeled by Claude against rubric prose without consulting the judge's prior scores. `npm run judge:calibrate` reports Spearman ρ vs Claude labels: **optimize 0.787 (MAE 3.20), suggestions 0.755 (MAE 2.95), span-labeling 0.688 (MAE 3.05)** — n=20 each. Optimize and suggestions pass the ρ ≥ 0.7 threshold; span-labeling misses by 0.012. Cause of the span-labeling miss: the sample spans two product versions of the labeler — entries 0-9 emit window-fragment outputs (scored 5-11), entries 10-19 emit clean semantic spans (scored 21-25). Disagreement with the judge concentrates in the rank ordering of the bifurcated middle. Sub-project D's product fix is expected to remove the version drift and bump ρ as a side effect; if it doesn't, opens Sub-project A2 for rubric iteration. Caveat: labels are LLM-authored (cross-model agreement check, not human trust anchor — the parent spec's original intent). B and C ship under "judge anchored at ρ 0.78/0.76, scores treated as judge-vs-judge agreement deltas." Sub-project A surfaced two real bugs in the calibration runtime fixed under the same work: (a) `run-calibration.ts` missing `import "dotenv/config"` — silent local failure with "Missing credentials" for `OPENAI_API_KEY`, CI unaffected because GH Actions injects vars directly; (b) `run-calibration.ts` parallelizing 20 entries via `Promise.all(entries.map(...))` — exceeded GPT-4o's 30k TPM rate limit, dropping ~80% of entries to 429s; serialized to a for-of loop. Both have regression tests. New principles encoded: _calibration thresholds become production-critical the moment they gate real decisions — a script that's silently broken locally is a script that nobody can iterate on rubrics with; the runtime correctness of measurement tooling matters as much as the rubric content._
- **2026-05-22 (Sub-project C7 — telemetry-schema fix + colon-tail variant + attribution proof):** C4 added `previewPrompt` to the synthetic driver's `trace.complete` payload. C5 added `skipCache: true` so the structuredArtifact branch would fire. But verification showed 0/18 events with `previewPrompt` populated despite `onMetadata` actually firing with non-empty preview strings. **Root cause:** `OptimizeEventProperties` schema at `server/src/services/observability/types.ts:43-66` is a whitelist of allowed properties; `previewPrompt` wasn't on the list, so `OptimizeTrace.complete` silently dropped it before emitting to PostHog. Fix: added `previewPrompt?: string \| null` to both `OptimizeTraceSummary` and `OptimizeEventProperties` types, plus `previewPrompt: summary.previewPrompt ?? null` to the properties object construction. **Verification proved the C5/C6 attribution chain end-to-end** (16/16 events post-fix with previewPrompt captured, last matrix run): PREVIEW shows aperture phrase in 16/16, "on Xmm" in 15/16 — LLM IS populating `camera_lens` and renderer IS composing `on {lens}`. OUTPUT shows aperture in 0/16 — compile step washes it out. **New variant discovered:** one OUTPUT started with `"Static tripod, eye-level, 100mm at: An extreme close-up..."` — colon-tail truncation at the BEGINNING of the prompt rather than end-of-prompt parenthetical. C6's guard only forbade end-of-prompt parenthetical tails; the colon-prefix variant slipped through. **Strengthened C6 universally:** `OUTPUT FORMAT RULES` now forbids both (a) end-of-prompt parenthetical/comma tech-spec tails AND (b) start-of-prompt colon-list tech-spec prefixes AND (c) orphaned prepositions like "...at" or "...with" as terminations. Regression test added for the colon-list variant. **Layer 10 of the false-signal hunt:** whitelist-based telemetry schemas silently drop fields that pass TypeScript's excess-property check but aren't in the runtime whitelist. The TypeScript type system says "this field exists on the type"; the runtime says "this field is included in emission" — two different contracts. When a sub-project adds telemetry, both the TypeScript type AND the emission whitelist must be updated, or the field is invisible to dashboards. New principle: _telemetry schema additions require type-AND-emission updates in lockstep. The TypeScript type-pass-through doesn't guarantee the field reaches PostHog; the property-construction object is the actual emission boundary._ Final attribution chain for the "lens at" fragment family: (1) Sub-project C addressed renderer slot duplication — different path; (2) Sub-project C6 addressed compile-step parenthetical tail — universal prompt rule; (3) Sub-project C7 closes the attribution gap by making `previewPrompt` visible AND addresses the colon-tail variant. The fragment family is now structurally fenced at three independent layers.
- **2026-05-22 (Sub-project C5 — refines "lens at" root cause):** Added `skipCache: true` to the synthetic optimize driver to force cache-miss execution and exercise the structuredArtifact branch. **Fresh run revealed the actual "lens at" mechanism** — surfaced in 1 of 18 outputs: the compile step appends a parenthetical tech-specs tail like `"(5s, 16:9, 24fps, 50mm at"` to its freeform-prose rendering, and that parenthetical CAN get truncated mid-phrase by max*tokens or the LLM's natural stopping behavior. **Sample event** (input: "A barista pours steamed milk creating a heart pattern in coffee"): the output ended with `"... soft chatter provide a delicate ambient soundscape. (5s, 16:9, 24fps, 50mm at"` — that's the same "lens at" fragment Sub-project D's labeling surfaced, but in a structurally-different location than Sub-project C addressed. **Refines the Sub-project C narrative:** C fixed the renderer's slot-composition path (sentence2's `on {camera_lens}`); the compile step's parenthetical tech-specs tail is a SECOND, independent code path that can produce the same fragment pattern via tech-specs serialization + truncation. Sub-project D's labeling didn't distinguish the two paths because both produce visually-identical fragments in the rendered text. **Open question (Sub-project C6 candidate):** locate the compile step's tech-specs tail serializer (likely in `VideoPromptCompilationService.serializePrompt` or its `optimizeForModel`-internal prompt template) and either (a) require completed tech-specs lines (no mid-phrase termination), (b) drop the parenthetical tail entirely when bullet-shaped output is unsafe, or (c) add a stop-sequence guard. **previewPrompt capture still not working** even with skipCache (0/18 events had previewPrompt populated) — onMetadata callback isn't reaching the synthetic driver despite being on the OptimizationRequest type. Possible type-pass-through issue between optimize.driver → PromptOptimizationService.optimize → runOptimizeFlow. Out of C5's scope; documented for whoever picks up the deeper compile-step investigation. New principle encoded: \_a failure pattern in the rendered output can be produced by multiple independent code paths. Sub-project C eliminated one (renderer slot duplication); the compile step's tech-specs tail produces the same visual pattern via a different mechanism. Single-path fixes don't generalize when the symptom is structural.* Layer 9: shared-symptom multi-path failure attribution.
- **2026-05-22 (Sub-project C4 — telemetry ship + cache-blocker finding):** Added `previewPrompt` capture to the synthetic optimize driver. The intent: compare the renderer's pre-compile output (`previewPrompt`, which includes `on {camera_lens}` when slot is populated) against `outputPrompt` (post-compile freeform) to disambiguate "LLM emits null" from "compile step strips it." **Capture mechanism:** dual-channel — `onMetadata` callback on `OptimizationRequest` (fires on cache miss) PLUS fallback from `response.metadata.previewPrompt` (cache-hit path). **Empirical result on the rerun:** 19/19 captured events have `previewPrompt: null`. **Cache-vs-fresh diagnostic:** `runOptimizeFlow` line 88-114 short-circuits to a cache-return path BEFORE building the structuredArtifact (lines 195-225) — and the `handleMetadata({ previewPrompt })` call only fires inside that branch. Cached metadata for the 20 synthetic prompts predates `previewPrompt` being in the metadata payload, so the cache-return code path's `metadata: cachedMetadata` lacks the field. The synthetic harness re-runs the same 20 prompts across matrix runs, hitting cache every time after the first. **The disambiguation question is unanswered:** can't tell if (a) LLM emits `camera_lens: null` OR (b) compile strips it. Both possibilities are still live. **Out of scope, opened C5:** either invalidate the relevant cache entries OR run synthetic with cache disabled (`OPTIMIZATION_CACHE_DISABLED=true` or equivalent) for a one-off fresh emission. The telemetry pipeline is now ready — C5 just needs to provoke a cache miss so the structuredArtifact branch fires once. **What shipped here:** previewPrompt column on every future `optimize.completed` event from the synthetic harness (zero impact on prod since prod's onMetadata is route-handler-defined). The cache-blocker finding itself is the lesson: _measurement infrastructure additions to a cache-fronted code path are invisible until cache invalidation; future telemetry changes should always include a cache-miss verification step in the same session_. Layer 8 of the false-signal hunt: stale cached metadata silently shadowing telemetry-payload additions.
- **2026-05-22 (Sub-project C3 — Layer 7 false-signal):** Investigation of C2's "LLM emits camera*lens: null" finding surfaced a structural measurement-system bug: **the synthetic harness was measuring a non-production code path**. `scripts/synthetic/drivers/optimize.driver.ts:67` set `useConstitutionalAI: true`, while the constitutional-review feature's own JSDoc at `server/src/services/prompt-optimization/workflows/constitutionalReview.ts:2-19` explicitly declares it dormant: *"Status: dormant. No production caller sets useConstitutionalAI: true — the optimize route handler does not forward the field, the client API does not send it, and no UI control exposes it. This workflow is reachable only from tests."_ Result: every Sub-project D calibration label, Sub-project C "lens at" fragment count, and Sub-project B2/C verification matrix run was measuring constitutional-review output, not the user-facing pipeline. Fixed the synthetic driver to `useConstitutionalAI: false` to mirror prod. **Re-verified optimize matrix post-fix:** still 0/20 outputs with aperture or "on Xmm" phrases. The constitutional-review-was-washing-it-out hypothesis is partially refuted — even without constitutional review, the LLM-provided camera_lens info doesn't reach the rendered output. Trace of `runOptimizeFlow` revealed another transformation layer: **Step 3 "Compile for target model"** (`VideoPromptCompilationService.compile()`) re-renders the structured-prompt artifact for the specific target model (sora/runway/kling/luma), and this step likely strips or paraphrases the renderer's `on {camera_lens}` phrase. Whether the LLM is also emitting `camera_lens: null` remains unverified (the telemetry only stores `outputPrompt`, not the structured slots). Opened C4 to investigate the compile-step transformation OR add slot-level telemetry to disambiguate. **This is Layer 7 of the measurement-system false-signal hunt** (1: canned harness; 2: rubric phantom-taxonomy; 3: calibration parallelization; 4: regex scorer; 5: fixture self-consistency; 6: pre-rewrite sample pollution; **7: synthetic-vs-prod behavioral divergence on dormant feature flags**). New principle encoded: \_measurement infrastructure must mirror production behavior exactly. When the synthetic harness enables a feature flag that no production code path enables, measurements may be optimizing the wrong pipeline. Audit `useConstitutionalAI` and any similar flags whenever a new sub-project's metric doesn't match the labeled-output's expected shape — the divergence is the bug, not the LLM's behavior._
- **2026-05-22 (Sub-project B3 — design ship):** Suggestions diversity (template-bound canned scaffolds). Sub-project D's calibration labeling surfaced three concrete templated-mode patterns: six "slow dolly {direction}" variants (camera.movement), the {tight,shallow}×{focus, rack focus, bokeh} matrix (camera.focus), and "soft {source}" lights (lighting.source). Investigation of `slotPolicies.ts` confirmed 8 of the engine's policies use `mode: "templated"`; the three labeled ones have a template-slot space too small relative to `targetCount: 6`, so picks repeat shapes within a single response. **Why this isn't a one-commit flip:** templated mode does load-bearing constraint enforcement — `camera.movement`'s `invalidCombinations` array structurally forbids `static {direction}` and `handheld locked-off frame`; a naive `mode: "guided_llm"` flip loses these. `EnhancementV2Engine.test.ts:103` explicitly asserts `execution.debug.mode === "templated"` and `modelCallCount === 0` for camera.movement. **Ship from this session:** design spec at [`docs/superpowers/specs/2026-05-22-suggestions-diversity-design.md`](../specs/2026-05-22-suggestions-diversity-design.md) presenting three options — A (expand template variety, lowest risk), B (hybrid templated→guided diversification, medium risk), C (full mode flip with rescued constraints, highest risk) — with recommendation Option A. Inline TODO comments added to the three named policies in `slotPolicies.ts` pointing at the spec so future operators see the link. **Out of scope (deferred):** full implementation of A/B/C — picks one in a future brainstorm session; ~1-2 days of focused work. New principle encoded: _when a labeled failure mode has multiple co-existing remediation options of different blast radius, ship the analysis + options + recommendation first as its own sub-project; commit to the implementation only after the brainstorm validates the chosen option. Premature implementation of complex multi-policy changes accumulates regression risk without de-risking the chosen approach._
- **2026-05-22 (Sub-project D2):** Investigation of why Sub-project D's reseed produced ρ=0.433/0.507/0.525 (all < 0.7) despite cleaner data. Score-distribution analysis of the labeled calibration entries confirmed the ceiling-effect hypothesis: **optimize** (n=20) `9 11 11 14 15 17 17 17 17 18 18 19 19 19 20 20 20 22 22 22` — spread 9-22, no ceiling cluster, MAE=5.85 reflects genuine absolute disagreement (23% of scale); **suggestions** (n=20) `14 14 14 14 17 18 19 19 19 19 20 20 20 20 21 21 23 23 24 24` — bimodal cluster at 14 + cluster at 19-21, MAE=3.45 (14% disagreement); **span-labeling** (n=20) `16 19 21 21 21 21 22 23 23 23 24 24 25 25 25 25 25 25 25 25` — **40% at the 25/25 ceiling**, MAE=1.25 (5% disagreement). Three surfaces, three different reasons for ρ < 0.7. The ρ threshold conflates them. Implemented in `run-calibration.ts`: now reports `rho`, `MAE`, `humanMean`, and `ceilingPct` together on every calibration run, and emits an explicit "rho is unreliable" NOTE when `ceilingPct > 30%`. Failure logic unchanged (still gates on ρ ≥ 0.7) — the operator gets the diagnostic context to know whether the gate is meaningful for the current distribution. Side-fix: added `scripts/quality-judge/calibration/__tests__/` to the `vitest.unit.config.js` include list — those tests (run-calibration regression + stratify) were silently excluded from `npm run test:unit` since Sub-project A landed. New principle encoded: _ρ alone is the wrong primary metric for ceiling-clustered surfaces. ρ + MAE + humanMean + ceilingPct together distinguish "rank disagreement on a healthy spread" from "absolute disagreement on bimodal data" from "rank-correlation artifact on a ceiling cluster" — these are three different conditions requiring three different responses (judge iteration vs. rubric refinement vs. accepting MAE as the primary signal). Future calibration sub-projects should report all four metrics; the ρ-only `[calibration] surface: rho=X` line is a single-axis view of a multi-axis problem._
- **2026-05-22 (Sub-project C2 — partial, finding):** Strengthened camera*lens slot description in `videoPromptOptimizationTemplate.ts`: changed `"Null is allowed when no specific lens preference"` to imperative `"REQUIRED: ... Always pick a concrete focal length + f-stop; only use null when the prompt explicitly rejects all lens-based DOF effects (extremely rare)"`. Updated OpenAI/Groq builder bullets symmetrically with "REQUIRED" + "ALWAYS a concrete value" + "NEVER null" reinforcement. **Matrix verification (openai-with-camera-lens, 20 events post-change):** still 0/20 outputs contain `f/X` or `on Xmm` patterns — the LLM continues to emit `camera_lens: null` despite the strengthened prompt. **Hypothesis surfaced by sample inspection:** the synthetic driver uses `useConstitutionalAI: true`, which routes the rendered prompt through a second LLM pass (`workflows/constitutionalReview.ts`). The constitutional review likely rewrites the renderer's composed sentence — including the `on {camera_lens}` phrase — into freeform prose that drops the slot-level lens detail. Sample outputs like "Static: An extreme close-up, eye-level shot of a single white wax candle..." show the constitutional-review style, not the renderer's "The camera uses {move} {angle} on {lens}..." structure. **Next step (Sub-project C3 candidate):** verify by running synthetic with `useConstitutionalAI: false` and checking if camera_lens info survives; if confirmed, the fix is updating constitutional review to preserve camera/lens content rather than paraphrase it. Until then: the prompt-language change is shipped (it can't hurt, may help non-constitutional code paths in dogfood/prod), but the matrix-measurable lift remains absent. New principle encoded: \_LLM output pipelines with multiple sequential rewrite stages can wash out earlier-stage signal. When a downstream "polish" pass exists, single-stage improvements may not survive end-to-end measurement; the right test variant disables downstream stages to isolate the change's effect.*
- **2026-05-22 (F5 + F6):** Telemetry-schema discriminator shipped. **F5:** PostHogClient.capture() now stamps a `harnessVersion` property onto every captured event, resolved at first call via `process.env.HARNESS_VERSION` (build-time injection) → `git rev-parse --short HEAD` (dev/local, memoized via `execFileSync` with argv-array form) → `"unknown"` (no env, no git). Env read fresh per capture so tests can override via beforeEach; git fallback memoized. Closes the lineage gap that Sub-project D's `MIN_EVENT_TIMESTAMP` cutoff worked around at the sampler side. **F6 (resolution):** Diagnostic counted 784 pre-cutoff polluted events (343 quality.scored, 220 suggestions.completed, 120 label-spans.completed, 101 optimize.completed) plus 1097 post-cutoff-pre-F5 events (legitimately null harnessVersion — Sub-projects A/B/C/D era). PostHog has no clean event-level property-update API; backfill would require re-emission (creates duplicates) or deletion (loses audit). Pre-launch context favors **document + defense-in-depth** over destructive backfill. `select-samples.ts` comment updated to record the migration plan: when post-cutoff-pre-F5 events age out of common lookback windows (~mid-June 2026), the sampler can retire `MIN_EVENT_TIMESTAMP` in favor of `properties.harnessVersion IS NOT NULL` as the canonical filter. Three principles encoded across F5/F6: (i) operational events must carry their process build identity — calibration samples can distinguish behavior boundaries (harness rewrites, prompt changes, schema migrations) without date heuristics; (ii) telemetry-schema decisions should be **fail-forward** — adding fields now prevents future archaeology, even when no current consumer needs them; (iii) when historical mutation is awkward (PostHog event immutability), prefer documenting the period-of-impurity over destructive cleanup that loses audit trail. Out of scope per pragmatic minimum: deleting or re-emitting historical events. The MIN_EVENT_TIMESTAMP filter remains canonical until June 2026.
- **2026-05-22 (Sub-project B2):** Imperative + one-shot scene*summary shipped. Sub-project B (2026-05-21) lifted suggestions relevance +0.30 but was capped by ~36% Qwen emission of `scene_summary`. A diagnostic this session refined the picture across all providers post-Sub-project-B: **qwen 39.7%, gemini 31.0%, default (Sub-project B baseline) 41.4%** — refuting the handoff's "Gemini reliable" framing. Investigation confirmed schema-enforcement is closed for both providers: Qwen via Groq doesn't support `json_schema` (`GroqQwenAdapter.ts:261` documents this explicitly), and Gemini's `responseSchema` is validation-based — required fields are advisory in practice (the `ProviderDetector.strictJsonSchema: true` flag is technically accurate but operationally misleading). Solution is provider-agnostic prompt engineering: imperative MUST + starts-with literal constraint (`Begin your response with the literal text {\"scene_summary\":`) + hard-requirement reframing + a one-shot EXAMPLE block demonstrating the `{scene_summary, suggestions[]}` shape with `camera.angle` content (chosen to avoid biasing typical highlights). Both `buildPrompt` and `buildCustomPrompt` receive the change — custom-request path previously had NO scene_summary instruction at all (explicit Sub-project B deferral, now reversed). `EnhancementV2Engine._generateGuidedCandidates` custom branch extended with `captureSiblings: true` to capture and surface `scene_summary` (was returning null unconditionally). `customSuggestion.ts` schemas mirrored from `enhancement.ts`: strict variant adds `scene_summary` to required as nullable-string; Groq variant adds it to properties only. **Matrix verification (qwen vs qwen-with-imperative on 58-event runs):** emission rate **39.7% → 60.3% (+20.6pp)**. Substantial lift, below the aspirational 70% target — Qwen via Groq still drops the field ~40% of the time, suggesting either prompt-mechanism saturation or a need for assistant-prefill at the json_object level (deferred to future sub-project). **Sub-project C matrix verification (deferred from C, bundled here):** new `openai-with-camera-lens` variant emitted 20 optimize events with **0 occurrences of the "lens at," or "lens at." fragment patterns** that appeared in ~50% of Sub-project D's labeled entries — the dominant failure mode is eliminated. Caveat surfaced: the LLM is emitting `camera_lens: null` rather than populating the new slot (0 of 20 outputs contain `f/X`), so the renderer falls back to `focusFromFraming`'s hardcoded aperture phrases. The fragment elimination still holds (no more duplication-induced compensation), but the *positive* lift (LLM-provided lens info making it to the output) is not yet realized. Follow-up: strengthen the prompt's encouragement of camera_lens emission, or add a few-shot example for the optimize template — separate Sub-project C2 work. Two new follow-ups documented: (i) Sub-project B3 (template-bound canned scaffolds in suggestions — Sub-project D's labeling surfaced six-variant "slow dolly" / "soft {source}" patterns that collapse diversity), (ii) F1 (Gemini JSON parse flakiness) — same Gemini-responseSchema-advisory root cause as B2's Gemini finding but the remediation is in JsonExtractor/repair logic, not the prompt layer. New principles encoded: \_provider capability flags can be operationally misleading. ProviderDetector's `strictJsonSchema: true` for Gemini is technically correct (Gemini supports `responseSchema` natively) but the runtime enforcement is advisory — required fields in `responseSchema` don't reliably make the model emit them. Verify schema-enforcement strength empirically (2-variant matrix on a non-trivial nullable field) before assuming flags describe production behavior. Also: when a fix eliminates a failure mode but doesn't produce the intended positive behavior (here, lens info making it through), distinguish "regression fixed" from "feature shipped" in the measurement entry — they're different outcomes that need different follow-ups.*
- **2026-05-22 (Sub-project C):** Optimize `camera_lens` slot shipped. Root-cause fix for the dominant failure mode Sub-project D surfaced in optimize calibration (~50% of entries contained "lens at," fragments — see the labeling subagent's reflection on optimize.calibration.json for the failure-pattern summary). Added `camera_lens: string | null` slot to `VideoPromptSlots` and both provider schemas (OpenAI strict in `required`, Groq in `properties` only — Groq treats `required` as advisory per Sub-project B's documented finding). Renderer composes `on {camera_lens}` into the camera sentence and suppresses the hardcoded `focusFromFraming` aperture phrase when the slot is present — eliminating the duplication the LLM was compensating for by truncating its own lens output. Linter validates non-null `camera_lens` for aperture marker or focal-length keyword (mm/prime/lens/anamorphic/telephoto/wide-angle/macro) and rejects dangling-preposition fragments; routes through the existing reroll mechanism via `isQualityVideoPromptLintError`. Template instruction split: `camera_move` (movement only, 3-8 words, explicit "do NOT include lens here") and `camera_lens` (focal length + aperture, with shot-type guidance). All 4 `VIDEO_FEW_SHOT_EXAMPLES` updated to demonstrate the slot boundary explicitly; `technical_specs.camera` description simplified to remove the lens duplication. OpenAI and Groq template-builder bullets updated symmetrically. Verification: 7 linter tests, 7 renderer tests, 3 schema-factory tests, 5 integration regression tests including a direct test for the labeled "anamorphic lens at" fragment. **Matrix-comparison measurement deferred:** the post-fix optimize mean against Sub-project D's 17.35 baseline requires a 2-variant matrix run (`openai` vs `openai-with-camera-lens` once a synthetic harness preset is added in `variants.ts`) — flagged as immediate follow-up. Out of scope per design (split into separate sub-projects): tag-soup tails in `style` slot (Sub-project C2), mid-sentence terminal truncations beyond the lens fragment (Sub-project C3, likely max*tokens-related). New principle encoded: \_LLM "compensation" failures — the model adjusting its output to avoid downstream duplication it senses coming — are often misread as truncation bugs; the actual fix is removing the duplication source, not raising max_tokens or adding stop sequences. When the LLM produces an obvious fragment, look for a downstream consumer (renderer, post-processor, formatter) that's about to repeat the field the model just stopped writing.*
- **2026-05-22 (Sub-project D / Layer 6):** Calibration reseed after the diagnostic session (agent `a6c5046dcaf48639c`) refuted Sub-project A's working hypothesis that span-labeling's ρ=0.688 measured a labeler under-segmentation bug. Actual story: the Sub-project A sampler (`scripts/quality-judge/calibration/select-samples.ts`) used a 7-day PostHog lookback when run on 2026-05-15, which spanned commit `4bf218dd` (2026-05-14T23:16:39Z) — the harness rewrite that replaced canned `synthesizeSpans()`/`synthesizeOptimize()`/`synthesizeSuggestions()` helpers with live `AIModelService` calls. Pre-`4bf218dd` events carried fabricated `provider="gemini"` annotations on canned window-fragment content; entries 0-9 of the original span-labeling calibration were Claude scoring those fake outputs. Fix: added `MIN_EVENT_TIMESTAMP = "2026-05-14T23:16:39Z"` const to `select-samples.ts` with `parseDateTimeBestEffort()` wrappers in both HogQL queries (sampler-side floor — defense-in-depth against future operators running it at a different date). Window expanded 7→14 days per spec § 4 mitigation because optimize and span-labeling had no fresh quality.scored events inside the 7-day window. Three parallel labeling subagents re-labeled 20 stubs per surface against the rubrics-only (no peeking at prior calibrations or judge scores). **New honest ρ / MAE / Claude-mean (n=20 each):** optimize ρ=0.433 MAE=5.85 mean=17.35 (range 9-22), suggestions ρ=0.507 MAE=3.45 mean=19.15 (range 14-24), span-labeling ρ=0.525 MAE=1.25 mean=22.9 (range 16-25). **Three findings the reseed forced into the open:** (a) Span-labeling, the surface that "looked broken" at ρ=0.688, is actually the strongest performer on clean data (mean 22.9, MAE 1.25 ≈ 5% of scale). Its lower ρ vs. MAE is a ceiling-effect artifact — 10 of 20 entries scored 25/25 from Claude, so rank correlation amplifies small judge disagreements that wouldn't matter elsewhere. There is no labeler bug. (b) Optimize, which passed Sub-project A's ρ threshold (0.787), has the worst clean-data mean (17.35) — confirming Sub-project C's tail-truncation hypothesis directly. The "lens at," fragment pattern appeared in roughly half the sampled entries; mid-sentence terminal truncations ("imbued with the serene yet dramatic glow of") in three entries are unrecoverable. (c) Suggestions' clean mean of 19.15 reveals a **new diversity-collapse pattern** orthogonal to Sub-project B2's emission-rate focus: template-bound canned scaffolds (six "slow dolly {direction}" variants from one entry, six "soft {source}" lights from another) compress diversity and qualityRange to 1-2 even when categoryFidelity and plausibility stay at 5. **Follow-up sub-projects (explicitly NOT silently re-folded into D):** (i) **D2** — investigate why clean calibration data drops ρ across all three surfaces. Hypotheses to test: rank correlation degrades on ceiling-clustered surfaces (span-labeling); MAE is a more robust signal than ρ when scores are bimodal (optimize, ρ=0.43 with MAE=5.85/25=23% disagreement is real, not artifact); the original 0.787/0.755/0.688 numbers were partly spurious agreement on flat-distribution fake content. (ii) **B3** (new — orthogonal to B2) — address suggestions template-bound diversity collapse; B2's emission-rate work won't fix it. **What this entry should change about how the next sub-project frames its measurement:** stop reporting ρ alone. Pair it with MAE and the Claude-mean — these three together distinguish "judge disagreement at the ceiling" (low ρ + low MAE + high mean) from "real product quality gap" (any ρ + high MAE + low mean). Span-labeling and optimize both have low ρ but completely different stories. New principle encoded: _measurement infrastructure has its own version-vs-data lineage problem; the events PostHog stores today don't know which version of the harness produced them, so the calibration sampler can silently sample across a behavior boundary it can't see. F5 (telemetry-schema discriminator: `harnessVersion` + `source: "synthetic-pool"`) and F6 (historical pre-rewrite event backfill) would fix this at the schema level; until they ship, sampler-side cutoffs like MIN_EVENT_TIMESTAMP are the workaround._
- **2026-05-14 (later):** Code review on the V2CandidateScorer rewrite caught 5 critical bugs in the new structural code. (a) `familyToCategoryId` used `split("_").join(".")` which silently mangled the 5 of 24 `SemanticFamily` values whose taxonomy IDs are camelCase — `lighting_time_of_day` → `lighting.time.of.day` (wrong) instead of `lighting.timeOfDay`. Replaced with an explicit `Record<SemanticFamily, string | null>` table. (b) `isCategoryRequiredMatch` had a parent-parent fallback that `isCategoryForbidden` lacked — net effect: a `technical.frameRate` candidate would simultaneously satisfy a `technical_aspect_ratio` required slot AND escape a `technical_frame_rate` forbidden slot. Dropped the parent-parent branch; both gates now symmetric. (c) Default Gemini model in harness was `gemini-2.0-flash` — prod uses `gemini-2.5-flash`, violating the project's "never invent provider/model values" rule. (d) Introduced `ABSTRACT_TERMS` Set + `containsAbstractTerm()` letter-bounded check as a "no-regex" replacement for the prior `ABSTRACT_DISALLOWED` regex — review correctly flagged this as the same wordlist-classification pattern the memory rule prohibits. Deleted entirely; trust the prompt's "Keep the replacement literal and camera-visible" rule. (e) Harness timeouts and limiters drifted from prod (separate limiters for groq+qwen; 6× longer groq timeout). Aligned. Added `__tests__/V2CandidateScorer.test.ts` with 37 table-driven tests over all 22 SemanticFamily values + parent/child semantics + grammar inference — would have caught bugs (a) and (b) at write time. Encoded principle: _every structural rewrite of a classifier needs table-driven coverage of the data table it replaces; without it, mechanical errors propagate silently._
- **2026-07-01 (ADR-0002 freeze propagation — active-loop-first rescope):** [ADR-0002](../../adr/0002-vidra-is-an-authoring-tool-for-non-experts.md) froze the generation-economics stack (credits, payment, video-job resilience) and the multi-shot/consistency stack (continuity, convergence) _after_ this program's 2026-05-12 "defer nothing" directive was written; the two were never reconciled until a 2026-07-01 grilling session. Resolution: **"defer nothing" now reads "defer nothing within the active authoring loop"** — frozen surfaces get instrumented at unfreeze, not before. Concretely: (a) **#2's route list drops continuity** — rescoped to motion, preview(+generation), model-intelligence, sessions, assets, reference-images, capabilities, storage; (b) **#5 goes dormant** — all 8 named workers belong to the frozen stacks; its "worker silent" alert concern revives with them; (c) **#6 unchanged** — dogfood signal on the active loop; (d) **#7 splits** — `costUsd` on `llm.call.completed` + preview/render events stays active, the Stripe revenue-movement slice freezes with #5. Two stale-status corrections landed in the same pass: calibration seeding was still listed as pending though it shipped via Sub-projects A/D/D2, and GH Actions secrets were re-verified absent (`total_count: 0` on 2026-07-01) — meaning **both** nightly quality workflows (`quality-judge.yml` AND `span-labeling-eval.yml`) have never run; reviving them needs `OPENAI_API_KEY`, `POSTHOG_API_KEY`, `POSTHOG_PERSONAL_API_KEY`, `POSTHOG_PROJECT_ID`, `POSTHOG_HOST`, `GROQ_API_KEY`. Propagated outward per this doc's protocol: `CLAUDE.md` § Active Programs updated (it was stale on both sub-project numbering and shipped status).

---

## Sub-project specs

- **#0** Eval Visibility — [`2026-05-10-eval-visibility-design.md`](../specs/2026-05-10-eval-visibility-design.md) (shipped)
- **#1** Source discriminator + synthetic-traffic harness — [spec](../specs/2026-05-10-source-discriminator-and-harness-design.md) · [plan](../plans/2026-05-10-source-discriminator-and-harness.md) (shipped — see `observability.md` for dashboards + tile IDs)
- **#2** Server route telemetry coverage (active-loop surfaces; rescoped 2026-07-01 per ADR-0002) — TBD (**ships next** per resequence)
- **#3** LLM-effectiveness eval framework (live judge) — [spec](../specs/2026-05-12-llm-judge-framework-design.md) · [plan](../plans/2026-05-12-llm-judge-framework.md) (**shipped 2026-05-12 → 2026-05-14**; code complete + multi-layer false-signal fixes; remaining: calibration JSON seeding + GH Actions secrets so the nightly cron runs)
- **#4** Cross-surface dashboards + alerts — TBD
- **#5** Background worker telemetry — dormant (ADR-0002 freeze; 2026-07-01)
- **#6** Client-side analytics (`posthog-js`) — TBD
- **#7** Cost / revenue telemetry — TBD

When a sub-project's spec lands, this row gains a link.

---

## How this doc keeps the program from dying

Solo measurement programs typically die one of three deaths: (a) the first sub-project ships, context drifts across sessions, and follow-ups never start; (b) follow-ups start but drift off-target because nobody re-reads the original vision; (c) sub-projects get bundled together for "efficiency" until the design collapses under its own weight.

This doc mitigates all three by externalizing the end state from any one spec, making it readable at the start of any future session in 30 seconds. Every sub-project spec opens with a pointer back here. When ambition shifts, the change happens **here first** — then propagates to in-flight sub-project specs — not the reverse. The Reordering log above is the audit trail; when priorities shift again, log them there before touching individual specs.

If you find yourself doing measurement work without having reread this file in the current session, that's the failure mode in progress. Re-read it now.

---

## Glossary — source discriminator values

The `source` property is part of sub-project #1's scope; eval events (#0) do not carry it because they have a distinct event name and aren't at risk of being conflated with user/synthetic traffic. Listed here for reference when #1 lands:

| Value       | When                                                                                               | Distinguishing characteristic                                                                |
| ----------- | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `user`      | Real authenticated or anonymous browser user — frontend sets `X-Telemetry-Source: user` explicitly | Header explicitly set by client API code; never inferred from absence.                       |
| `synthetic` | Requests fired by the pre-release synthetic-load script (introduced in #1)                         | Script sets `X-Telemetry-Source: synthetic` header.                                          |
| `dogfood`   | Authenticated team member using the app for internal testing                                       | Firebase UID matches a configured allowlist.                                                 |
| `ci`        | Requests fired from a CI job exercising real endpoints                                             | `CI=true` env var or a CI-job header.                                                        |
| `dev`       | Default when `NODE_ENV !== "production"` and no other rule matches                                 | Environment-derived; informative for local dev work.                                         |
| `unknown`   | Default when `NODE_ENV === "production"` and no other rule matches                                 | Environment-derived; flags a tagging gap to investigate (real-user calls should set `user`). |

---

## Related docs

- [`docs/architecture/observability.md`](../../architecture/observability.md) — source-of-truth reference for already-shipped event schemas (`optimize.completed`, `llm.call.completed`, `suggestions.completed`)
- [`docs/superpowers/specs/2026-05-09-t2v-optimize-telemetry-design.md`](../specs/2026-05-09-t2v-optimize-telemetry-design.md) — foundation spec; pattern subsequent sub-projects replicate
- [`docs/superpowers/specs/2026-05-10-eval-visibility-design.md`](../specs/2026-05-10-eval-visibility-design.md) — sub-project #0 design
- `CLAUDE.md` § "Active Programs" — entry point for any agent in a fresh session
