# Auto-generated files — do not reformat
docs/architecture/architecture-map.json

# Agent session scratch. Gitignored, so CI never sees it; without this the
# local format:check reports a failure CI cannot reproduce.
.remember/
