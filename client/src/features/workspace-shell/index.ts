/**
 * Workspace-shell public API.
 *
 * The only cross-feature import path is CanvasWorkspace; everything else
 * (events, editor-surface types) is imported by concrete module path.
 */
export { CanvasWorkspace } from "./CanvasWorkspace";
